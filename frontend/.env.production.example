# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES - SUNUCU DEPLOYMENT ŞABLONU
# =============================================================================
# Bu dosya docker-compose.prod.yml build arg'ları ile kombine edilir

# API URL - Docker container'lar arasındaki iletişim
# Docker network'te container adı ile erişilebilir
REACT_APP_API_URL=http://api:5000/api

# Backend servisi etkin mi?
REACT_APP_BACKEND_ENABLED=true

# Mock veri kullan mı? (development sırasında true yapılabilir)
REACT_APP_USE_MOCK_DATA=false

# Auth zorunlu mu?
REACT_APP_AUTH_ENABLED=false

# =============================================================================
# DOCKER DEPLOYMENT SENARYOLARI:
#
# SEÇENEK 1: Docker Container'lar Arası (Önerilen)
# REACT_APP_API_URL=http://api:5000/api
# - api service adı docker-compose.prod.yml'de kullanılır
# - Docker network'te otomatik resolve edilir
#
# SEÇENEK 2: Sunucu IP Adresi (IP sabit değilse sorunlu olabilir)
# REACT_APP_API_URL=http://31.186.24.78:5000/api
#
# SEÇENEK 3: Domain Adı (HTTPS + SSL gerektirir)
# REACT_APP_API_URL=https://golkoygurme.com.tr/api
#
# SEÇENEK 4: localhost (sadece local test için)
# REACT_APP_API_URL=http://localhost:5000/api
#
# =============================================================================
# KULLANIŞI:
# 1. Sunucuya kopyala:
#    scp frontend/.env.production.example huseyinadm@31.186.24.78:~/eticaret/frontend/
#
# 2. Sunucuda isimlendirmeyi değiştir:
#    mv ~/eticaret/frontend/.env.production.example ~/eticaret/frontend/.env.production
#
# 3. Kontrol et:
#    cat ~/eticaret/frontend/.env.production
#
# 4. Build et:
#    cd ~/eticaret
#    docker-compose -f docker-compose.prod.yml build --no-cache frontend
#
# 5. Deploy et:
#    docker-compose -f docker-compose.prod.yml up -d frontend
# =============================================================================
