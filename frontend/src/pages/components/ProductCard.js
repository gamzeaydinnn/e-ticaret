 // ürün kutucuğu (listelerde)

 /*import React from 'react';
import { Link } from 'react-router-dom';
export default function ProductCard({ product }){
  return (
    <div className="bg-white rounded shadow p-3">
      <Link to={`/product/${product.id}`}>
        <img src={product.images?.[0] || product.image} className="w-full h-40 object-cover rounded" />
        <h3 className="mt-2 font-semibold">{product.name}</h3>
        <div className="mt-1 text-lg font-bold">₺{product.price}</div>
      </Link>
      <button className="mt-3 w-full bg-blue-600 text-white p-2 rounded">Sepete Ekle</button>
    </div>
  );
}
*/